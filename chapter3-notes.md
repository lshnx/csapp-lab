# CS:APP 第三章笔记 — 程序的机器级表示

> 基于 CS:APP 3rd Edition。第三章是整本书最核心的一章——你把 C 代码编译成汇编，然后逐条看懂 CPU 在干什么。这是「软件和硬件的分界线」落到实处的章节。

---

## 3.1 历史视角

Intel 处理器演化：**8086（1978）→ 80286 → i386（1985）→ i486 → Pentium → Core → x86-64（2003）**

只需要知道三件事：

1. **x86-64 是今天你电脑的指令集。** 你的 CPU 说的就是这种语言。
2. **向后兼容。** 1978 年 8086 上能跑的指令，今天的 x86-64 CPU 照样能跑。这是 Intel 最成功的商业策略，也是指令集越来越臃肿的原因。
3. **i386 是第一个 32 位架构**，x86-64 是 64 位扩展——寄存器从 32 位扩到 64 位，数量翻倍（8 → 16 个）。

---

## 3.2 程序编码

### 你写的 C → CPU 看到的机器码，经过四步

```
hello.c  →  预处理  →  编译  →  汇编  →  链接  →  可执行文件
             (cpp)     (cc1)    (as)     (ld)
```

但实际中我们只关注两条命令：

| 命令 | 做什么 | 输出 |
|------|--------|------|
| `gcc -Og -S hello.c` | 编译成汇编代码 | `hello.s`（文本文件，人类可读） |
| `gcc -Og -c hello.c` | 编译 + 汇编成机器码 | `hello.o`（二进制文件，CPU 可执行） |
| `objdump -d hello.o` | 把 `.o` 反汇编回汇编 | 机器码 + 汇编对照 |

> `-Og` = 开启基础优化但不影响调试。不要用 `-O0`（完全不优化）——生成的代码充斥无用 load/store，读起来痛苦。

---

### hello.s 文件的结构——以你亲手生成的为例

```asm
        .file   "hello.c"          ← 汇编器指令：源文件名
        .text                       ← 汇编器指令：以下是代码段

.LC0:                               ← 标签：字符串数据
        .ascii "hello\0"            ← 汇编器指令：存字符串

        .globl  main                ← 汇编器指令：main 是全局符号
        .def    main; ...
        .seh_proc main

main:                               ← 函数入口
        subq    $40, %rsp           ← CPU 指令：分配栈空间
        call    __main              ← MinGW 内部初始化
        leaq    .LC0(%rip), %rcx    ← CPU 指令：加载字符串地址到参数寄存器
        call    puts                ← CPU 指令：调用 puts 输出
        movl    $0, %eax            ← CPU 指令：return 0（%eax = 返回值）
        addq    $40, %rsp           ← CPU 指令：回收栈空间
        ret                         ← CPU 指令：返回
```

---

### 两类内容——必须一眼能区分

| 类型 | 开头 | 例子 | 谁用 |
|------|------|------|------|
| **汇编器指令** | `.` | `.file` `.text` `.globl` `.ascii` | 给工具链（as/ld）看的元信息 |
| **CPU 指令** | 操作码 | `subq` `call` `movl` `leaq` `ret` | CPU 真正执行的指令 |

---

### 你看到的关键寄存器

| 寄存器 | 全名 | 在你 hello 里的角色 |
|--------|------|-------------------|
| `%rsp` | Stack Pointer（栈指针） | 指向栈顶——函数进来先挪栈（`subq`），出去再还原（`addq`） |
| `%rcx` | 第 4 个参数寄存器 | 装 `"hello"` 的地址，传给 `puts` |
| `%eax` | 返回值（Return Value） | `return 0` → `%eax = 0` |

---

### 编译器的「自作主张」——你写 printf，它用 puts

```c
printf("hello\n");
```

编译器发现：格式字符串里没有 `%d` `%s` `%f` 等占位符，参数只有一个纯字符串。`printf` 比 `puts` 贵（要多解析格式字符串），所以帮你优化成 `puts`。

> 这是你第一次亲眼看到编译器优化——后面会遇到更多。学会读 `gcc -S` 的输出是理解 C 代码性能的第一手资料。

---

### 知识检查

能回答这三个问题，3.2 就算过了：

| 问题 | 答案 |
|------|------|
| `hello.s` 和 `hello.o` 的区别？ | `.s` 是文本汇编，`.o` 是二进制机器码 |
| `objdump -d` 输出什么？ | 机器码 + 反汇编出来的指令（不依赖源代码） |
| 为什么 `printf("hello\n")` 变成了 `call puts`？ | 编译器优化——没有格式参数时 `puts` 更快 |

---

---

## 3.3 数据格式

Intel 用**指令后缀**标记操作数大小——这是你和汇编打交道时必须立刻认出的第一个特征：

| C 类型 | Intel 后缀 | 大小 | 举例 |
|--------|-----------|------|------|
| `char` | `b`（byte） | 1 字节 | `movb` `addb` |
| `short` | `w`（word） | 2 字节 | `movw` `addw` |
| `int` / `long`（32 位） | `l`（long word） | 4 字节 | `movl` `addl` |
| `long`（64 位）/ 指针 | `q`（quad word） | 8 字节 | `movq` `addq` |
| `float` | `s`（single） | 4 字节 | `movss` `addss` |
| `double` | `l`（long） | 8 字节 | `movsd` `addsd` |

> ⚠️ `l` 后缀既表示 4 字节整数又表示 8 字节浮点数——根据指令类型区分。看指令名就行，不会混淆。

**这就是你昨天 hello.s 里 `subq` `movl` `addq` 「q」「l」后缀的来源。**

---

### 寄存器命名——同一个寄存器怎么访问不同大小的部分

以 `%rax` 为例，64 位寄存器可以访问不同子集：

```
%rax ───────────────────────────────  64 位（8 字节）
              └────── %eax ────────   低 32 位（4 字节）
                      └── %ax  ───   低 16 位（2 字节）
                      └─ %ah ─       高 8 位（旧式）
                        └─ %al ─     低 8 位（1 字节）
```

**关键规则**：操作 `%eax` 时，高 32 位**自动清零**。这也是为什么 `movl $0, %eax` 等价于 `movq $0, %rax`——前者还省一个字节的指令编码。

| 你要操作 | 用哪个名字 | 举例 |
|---------|-----------|------|
| 整个 64 位（存指针） | `%rax` | `movq $0x1000, %rax` |
| 低 32 位（存 int） | `%eax` | `return 0` → `movl $0, %eax` |
| 低 16 位（基本不用） | `%ax` | — |
| 低 8 位（单个 char） | `%al` | `movb $65, %al` |

> 注：`%ah`（高 8 位）在 x86-64 中兼容性受限，新代码基本不用。

---

### x86-64 的 16 个通用寄存器

| 64 位 | 32 位 | 16 位 | 8 位 | 用途约定 |
|-------|-------|-------|------|---------|
| `%rax` | `%eax` | `%ax` | `%al` | 返回值 |
| `%rbx` | `%ebx` | `%bx` | `%bl` | 被调用者保存 |
| `%rcx` | `%ecx` | `%cx` | `%cl` | 第 4 个参数 |
| `%rdx` | `%edx` | `%dx` | `%dl` | 第 3 个参数 |
| `%rsi` | `%esi` | `%si` | `%sil` | 第 2 个参数 |
| `%rdi` | `%edi` | `%di` | `%dil` | 第 1 个参数 |
| `%rbp` | `%ebp` | `%bp` | `%bpl` | 基址指针 |
| `%rsp` | `%esp` | `%sp` | `%spl` | **栈指针** |
| `%r8`–`%r15` | `%r8d`–`%r15d` | `%r8w`–`%r15w` | `%r8b`–`%r15b` | 第 5–6 个参数 + 通用 |

> 🔑 记住 `%rsp`（栈指针）、`%rax`（返回值）、`%rdi`–`%rdx`（前 3 个参数）就够应付大多数场景。

---

---

## 3.4 访问信息

### 3.4.1 操作数指示符 —— 今天最核心的知识点

一条 `mov` 指令 = 三个部分：

```
movq  源操作数,  目的操作数
```

数据来源/去向只有三种类型：

| 类型 | 形式 | 含义 | 例子 |
|------|------|------|------|
| **立即数** | `$常数` | 就是常数本身（嵌在指令里） | `$42` `$0x1A` `$-5` |
| **寄存器** | `%reg` | 寄存器的值 | `%rax` `%rsp` `%eax` |
| **内存引用** | `偏移(基址, 变址, 比例)` | 从内存某个地址读/写 | `(%rax)` `-8(%rbp)` |

---

#### 内存寻址的完整公式

```
Imm(rb, ri, s)  →  有效地址 = Imm + R[rb] + R[ri] × s
```

| 组件 | 含义 | 是否必须 |
|------|------|---------|
| `Imm` | 常数偏移（立即数） | 否，省略 = 0 |
| `rb` | 基址寄存器（64 位） | 否，但四个至少一个 |
| `ri` | 变址寄存器（64 位） | 否 |
| `s` | 比例因子（1、2、4、8） | 否，必须和 `ri` 同时出现或省略 |

> `s` 只能是 1、2、4、8——对应 char/short/int/long 的大小。这是为数组索引设计的。

#### 常见内存寻址速查

| 形式 | 地址 = | 用在 |
|------|--------|------|
| `(%rax)` | `R[%rax]` | 解引用指针 `*ptr` |
| `8(%rsp)` | `R[%rsp] + 8` | 访问栈上第 2 个局部变量 |
| `-8(%rbp)` | `R[%rbp] - 8` | 访问栈帧中较低位置的变量 |
| `(%rax, %rcx, 4)` | `R[%rax] + R[%rcx] × 4` | 数组 `arr[i]`，`%rcx` = i，每个元素 4 字节 |
| `arr(%rip)` | `R[%rip] + arr`（相对 RIP） | 访问全局数据 / 跳转表 |

> 🔑 `8(%rsp)` 在过程调用中大量出现——每次 `call`/`ret` 都在和它打交道。3.7 节会深入。

---

### 3.4.2 数据传送指令

#### mov 家族

| 指令 | 效果 | 大小 |
|------|------|------|
| `movb S, D` | D ← S | 1 字节 |
| `movw S, D` | D ← S | 2 字节 |
| `movl S, D` | D ← S | 4 字节，且 D 高 32 位清零 |
| `movq S, D` | D ← S | 8 字节 |
| `movabsq I, R` | R ← I | 64 位立即数 → 寄存器 |

**核心规则**：源和目的**不能同时是内存地址**——必须至少有一个是寄存器或立即数。

```
movq (%rax), (%rbx)   ← ❌ 非法！内存→内存
movq (%rax), %rcx     ← ✅ 正确。内存→寄存器
movq %rcx, (%rbx)     ← ✅ 正确。寄存器→内存
```

> 原因：x86-64 指令集设计上不支持一条指令做两次内存访问。要搬数据，先搬到寄存器中转。

#### 零扩展传送（高位补 0）

| 指令 | 从 | 到 | 说明 |
|------|----|----|------|
| `movzbw` | 1 字节 | 2 字节 | |
| `movzbl` | 1 字节 | 4 字节 | 最常用——char → int |
| `movzwl` | 2 字节 | 4 字节 | |
| `movzbq` | 1 字节 | 8 字节 | |
| `movzwq` | 2 字节 | 8 字节 | |

> `movzbl` 把 `unsigned char` 转成 `int` 时出现——大小端无关、类型提升都靠它。

#### 符号扩展传送（高位补符号位）

| 指令 | 从 | 到 | 说明 |
|------|----|----|------|
| `movsbw` | 1 字节 | 2 字节 | |
| `movsbl` | 1 字节 | 4 字节 | |
| `movswl` | 2 字节 | 4 字节 | |
| `movsbq` | 1 字节 | 8 字节 | |
| `movswq` | 2 字节 | 8 字节 | |
| `movslq` | 4 字节 | 8 字节 | `int` → `long` 时出现 |
| `cltq` | `%eax` | `%rax` | 专用于 `%rax` 的符号扩展 |

> 🔑 `movzbl` vs `movsbl`：前者补 0（无符号），后者补符号位（有符号）。`(char)-1` 用 `movzbl` 变成 255，用 `movsbl` 变成 -1——完全不同的结果。

> ⚠️ 压栈/弹出（`pushq` / `popq`）在 §3.4.4 讲。读到那里再补笔记。

---

### 3.4.1-3.4.2 自检

| 问题 | 答案 |
|------|------|
| 三种操作数类型？ | 立即数（`$常数`）、寄存器（`%reg`）、内存引用（`Imm(%rb,%ri,s)`） |
| `8(%rsp, %rcx, 4)` 的地址怎么算？ | `R[%rsp] + 8 + R[%rcx] × 4` |
| `movzbl` 和 `movsbl` 的区别？ | 零扩展（高位补 0）vs 符号扩展（高位补符号位） |
| `movl $0, %eax` 为什么比 `movq $0, %rax` 更高效？ | `%eax` 操作自动清零高 32 位，且指令编码少 1 字节 |
| 为什么不能 `movq (%rax), (%rbx)`？ | x86-64 不允一条指令两次内存访问——需经过寄存器中转 |

---

---

## 3.4.3 数据传送示例

书上给了 `exchange` 函数的例子——两个指针参数的交换。

```c
long exchange(long *xp, long y) {
    long x = *xp;
    *xp = y;
    return x;
}
```

编译成汇编：

```asm
exchange:
    movq    (%rdi), %rax    # 从 *xp 读旧值到返回值
    movq    %rsi, (%rdi)    # 把 y 写进 *xp
    ret                      # 返回（旧值已在 %rax 里）
```

逐行解释：

| 行 | 做了什么 | 寄存器/内存 |
|----|---------|------------|
| `movq (%rdi), %rax` | 读内存 `*xp` → 返回值 | `%rdi` = xp（第 1 个参数，指针），`(%rdi)` = 解引用 |
| `movq %rsi, (%rdi)` | 写 y → `*xp` | `%rsi` = y（第 2 个参数） |
| `ret` | 返回 | `%rax` = 旧值 |

**两个关键观察**：

1. **C 的"指针" = 汇编的"间接寻址"。** `*xp` 翻译成 `(%rdi)`，`xp` 翻译成 `%rdi`。
2. **返回值走 `%rax`。** 函数返回时 `%rax` 里的值就是 C 代码的 `return` 值。

> 这个例子只有两条 mov + 一条 ret——最简单的函数。后面 3.7 节会看到复杂函数（参数多、局部变量、调用子函数）怎么翻译。

---

---

## 3.4.4 压入和弹出栈数据

栈是程序执行的核心数据结构。**栈向低地址增长**，栈顶由 `%rsp` 指向。

```
高地址
┌────────────┐
│   栈底      │  ← 函数刚进来时 %rsp 在这里（高地址）
│   ...      │
│   数据      │
│   ...      │
│   栈顶      │  ← 当前 %rsp（低地址）
└────────────┘
低地址
```

### pushq S — 把数据压入栈

等价于两步：

```
subq $8, %rsp       # 栈指针减 8（栈向下长）
movq S, (%rsp)      # 把 S 写到新的栈顶位置
```

### popq D — 从栈弹出数据

等价于两步：

```
movq (%rsp), D      # 从栈顶读到 D
addq $8, %rsp       # 栈指针加 8（回收栈空间）
```

### 为什么 pushq/popq 比两步分开快？

- `pushq` 和 `popq` 都只占 **1 个字节**的指令编码
- 手动 `subq + movq` 要 8+ 字节
- 栈操作极其频繁 → 省指令字节 = 省指令 cache

### 栈的用处（后面各节展开）

| 场景 | 用到栈的什么 | 在哪节 |
|------|------------|--------|
| 过程调用 | `call` 压返回地址，`ret` 弹出返回地址 | §3.7 |
| 参数传递 | 超过 6 个参数的部分放在栈上传给子函数 | §3.7.4 |
| 局部变量 | 寄存器不够用时，局部变量暂存在栈上 | §3.7.5 |
| 被调用者保存 | `%rbx` `%rbp` `%r12-%r15` 用了要保存旧值到栈上 | §3.7.5 |
| 缓冲区溢出 | 局部数组越界覆盖了栈上的返回地址 | §3.10 |

---

---

## 3.5 算术和逻辑操作

> 这一节从"搬数据"（mov）切换到"算数据"。指令逐渐变多，但规律一致——后缀 `b/w/l/q` 标记大小，两个操作数 `S, D` = 源和目的。

---

### 3.5.1 加载有效地址（LEA）

`leaq` 是整本书最容易被误解的指令——它长得很像 mov，但**不访问内存**。

```
leaq S, D    →    D = &S（把 S 的地址放进 D，不读 *(S)）
```

对比：

```asm
movq 8(%rdx), %rax      # %rax = *(rdx + 8)  —— 读内存
leaq 8(%rdx), %rax      # %rax = rdx + 8     —— 纯算术，不碰内存
```

**LEA 不读内存。它只是"假如这是一个地址，它等于多少"。**

#### LEA 的三种用法

| 用法 | 例子 | 效果 |
|------|------|------|
| 简单加法 | `leaq (%rdi, %rsi), %rax` | `%rax = x + y` |
| 乘加 | `leaq (%rdi, %rsi, 4), %rax` | `%rax = x + 4y` |
| 常数偏移 | `leaq 8(%rdi, %rdi, 2), %rax` | `%rax = 3x + 8` |

> 编译器为什么喜欢 LEA？一条 `leaq` 在一个时钟周期内做 `a + b×k + c`——比分开用 `mov + add + imul` 快。你在汇编里看到一长串 `leaq`，通常是编译器在用这招优化乘法。

---

### 3.5.2 一元和二元操作

#### 一元操作（只有一个操作数）

| 指令 | 效果 | 相当于 C |
|------|------|---------|
| `incq D` | D += 1 | `x++` |
| `decq D` | D -= 1 | `x--` |
| `negq D` | D = -D | `x = -x` |
| `notq D` | D = ~D | `x = ~x` |

#### 二元操作（两个操作数）

格式：`op S, D` → `D = D op S`

| 指令 | 效果 | 相当于 C |
|------|------|---------|
| `addq S, D` | D = D + S | `x += y` |
| `subq S, D` | D = D - S | `x -= y` |
| `imulq S, D` | D = D × S | `x *= y` |
| `andq S, D` | D = D & S | `x &= y` |
| `orq S, D` | D = D \| S | `x \|= y` |
| `xorq S, D` | D = D ^ S | `x ^= y` |

> ⚠️ 注意减法顺序：`subq S, D` = `D - S`，不是 `S - D`。Intel 语法是目的操作数在前。

#### 例子

```c
long arith(long x, long y) {
    long t1 = x + y;
    long t2 = t1 * 3;
    return t2;
}
```

```asm
arith:
    leaq    (%rdi, %rsi), %rax    # t1 = x + y（用 LEA 加法）
    leaq    (%rax, %rax, 2), %rax # t2 = t1 * 3 = t1 + t1*2
    ret
```

> 两条 LEA、零条乘法指令。`t2 = t1*3` 被翻译成 `t1 + t1×2`，比 `imulq` 快。这是编译器优化的经典例子。

---

### 3.5.3 移位操作

| 指令 | 效果 | 说明 |
|------|------|------|
| `salq k, D` | D = D << k | 左移（arithmetic left = logical left） |
| `shlq k, D` | D = D << k | 同 `salq`，完全一样 |
| `sarq k, D` | D = D >> k（算术右移） | 高位**补符号位** |
| `shrq k, D` | D = D >> k（逻辑右移） | 高位**补 0** |

**移位量 k 的规则**：可以是立即数（8 位），或者放在 `%cl` 寄存器里。不能是其他寄存器。

```asm
movb  $4, %cl
salq  %cl, %rax    # %rax <<= 4
```

> `salq` 和 `shlq` 机器码完全一样——x86-64 中左移只有一种，算术和逻辑没区别（低位都补 0）。

---

### 3.5.1-3.5.3 自检

| 问题 | 答案 |
|------|------|
| `leaq 8(%rdi,%rdi,4), %rax` 算出什么？ | `R[%rdi]×5 + 8`（x + 4x + 8） |
| `subq %rcx, %rax` 是 `%rax = %rax - %rcx` 还是反过来？ | `%rax - %rcx`，目的在前 |
| `imulq $3, %rax` 合法吗？ | **两操作数形式**不合法——它只能操作寄存器或内存，不能带立即数。但 x86-64 还有**三操作数形式** `imulq $imm, S, D`（D = S × imm），那个可以。CS:APP 本阶段只用两操作数形式，乘常数交给 LEA |
| `salq` 和 `shlq` 有什么区别？ | 没有——机器码完全一致 |
| 为什么编译器用 LEA 而不是 `imulq`？ | LEA 一个周期完成乘加，`imulq` 要多拍 |

---

### 3.5.4 讨论

这一节没有新指令，讲的是**编译器怎么把这些指令组合起来**。

**乘常数几乎不用乘法指令**：编译器会把「乘常数」拆成 LEA + 移位 + 加减的组合（§2.3.6 讲过数学依据：`x·K = x·2^a ± x·2^b ± ...`）。比如 `x * 14 = (x << 4) - (x << 1)`。

**循环展开（loop unrolling）**：书里用多项式求值的例子展示，gcc -O1 会把循环体展开，用两个独立变量分别累加——因为现代 CPU 有**多个功能单元**（几个加法器、几个乘法器），互相独立的指令可以在不同单元上**同时执行**（指令级并行，第 5 章会展开讲）。

> 核心结论：**汇编代码不是 C 代码的逐句翻译**。同一个函数，-Og 和 -O1 生成的汇编可能长得完全不一样——它是优化器权衡后的产物。这也是为什么初学用 -Og：它只做最直观的翻译，-O2 的代码经常"认不出来"。

---

### 3.5.5 特殊的算术操作

**动机**：两个 64 位整数相乘会得到 128 位结果；128 位 ÷ 64 位也超出普通指令的能力。x86-64 的解决方案：把 `%rdx` 和 `%rax` 拼成一个 16 字节的寄存器对（oct word，八字），`%rdx` 放高 64 位，`%rax` 放低 64 位。

| 指令 | 效果 | 说明 |
|------|------|------|
| `imulq S` | `%rdx:%rax` = S × `%rax` | **单操作数**！有符号全乘法（128 位结果） |
| `mulq S` | `%rdx:%rax` = S × `%rax` | 无符号全乘法 |
| `cqto` | `%rdx` = `%rax` 的符号扩展 | 为除法准备被除数 |
| `idivq S` | `%rdx:%rax` ÷ S → 商 `%rax`，余数 `%rdx` | 有符号除法 |
| `divq S` | `%rdx:%rax` ÷ S → 商 `%rax`，余数 `%rdx` | 无符号除法 |

> ⚠️ **`imulq` 有两副面孔**——注意区分：
> - 双操作数 `imulq S, D`（§3.5.2）：普通乘法，只保留**低 64 位**
> - 单操作数 `imulq S`（本节）：完整乘法，**128 位结果** → 高 64 位 `%rdx`，低 64 位 `%rax`
>
> 普通 C 乘法用双操作数就够，因为 C 本来就是只保留低 64 位（§2.3.4：乘积被截断）。而截断后低 64 位的位模式，有符号和无符号乘积**完全相同**——所以一条 `imulq` 通吃两种语义。

**`cqto` 的怪名字**：全称 "convert quad to oct"（四字转八字）——把 `%rax` 按符号位扩展成 128 位放进 `%rdx:%rax`。它**没有操作数**，因为固定操作 `%rax` 和 `%rdx` 这两个寄存器。IA32 时代它叫 `cltd`。gcc 会在除法前自动插入它。

**除法前必须准备好 `%rdx`**（被除数的高 64 位）：
- 有符号：`cqto`（符号扩展——负数时高 64 位全 1，这正好是负数的 128 位补码表示，和 §2.2.6 的符号扩展一脉相承）
- 无符号：`xorl %edx, %edx`（清零——任何数无符号扩展到 128 位，高半都是 0）

> 经典 bug：`idivq` 前忘了 `cqto`，`%rdx` 里是垃圾值，被除数整个是错的——结果错或直接触发异常。

**例子 1：128 位无符号乘积**

```c
#include <inttypes.h>
typedef unsigned __int128 uint128_t;
void store_uprod(uint128_t *dest, uint64_t x, uint64_t y) {
    *dest = x * (uint128_t) y;
}
```

```asm
# dest 在 %rdi，x 在 %rsi，y 在 %rdx
store_uprod:
    movq    %rsi, %rax     # 把 y 复制到 %rax（mulq 的固定操作数）
    mulq    %rdx           # %rdx:%rax = y × x
    movq    %rax, (%rdi)   # 存低 8 字节
    movq    %rdx, 8(%rdi)  # 存高 8 字节
    ret
```

注意：`%rdx` 上一秒还是"第 3 个参数寄存器"，这一秒就成了乘积的高 64 位——**寄存器角色随指令切换**。

**例子 2：同时返回商和余数**

```c
void remdiv(long x, long y, long *qp, long *rp) {
    long q = x / y;
    long r = x % y;
    *qp = q;
    *rp = r;
}
```

```asm
# x 在 %rdi，y 在 %rsi，qp 在 %rdx，rp 在 %rcx
remdiv:
    movq    %rdx, %r8      # 先把 qp 复制到 %r8——%rdx 马上要被占用
    movq    %rdi, %rax     # 被除数低 64 位 = x
    cqto                   # 被除数高 64 位 = x 的符号扩展
    idivq   %rsi           # 除以 y：商 → %rax，余数 → %rdx
    movq    %rax, (%r8)    # *qp = q
    movq    %rdx, 8(%r8)   # *rp = r
    ret
```

第 1 行是本节最值得品的细节：`qp` 本来在 `%rdx`，但 `idivq` 要把 `%rdx:%rax` 当被除数——所以编译器先把 `qp` 挪到 `%r8` 腾位置。

**两个坑**：
1. 除以 0 → `SIGFPE` 异常（"浮点异常"这名字纯属历史遗留，整数除 0 也发它）
2. 商装不下也会异常——比如 `INT_MIN / -1` = 2⁶³，超出 `long` 的范围

---

### 3.5.4-3.5.5 自检

| 问题 | 答案 |
|------|------|
| `imulq S, D` 和 `imulq S` 有什么区别？ | 双操作数只保留低 64 位；单操作数输出完整 128 位到 `%rdx:%rax` |
| 为什么一条 `imulq S, D` 能同时算有符号和无符号乘法？ | 乘积的低 64 位完全相同（§2.3.4 截断） |
| `cqto` 是干什么的？为什么没有操作数？ | 把 `%rax` 符号扩展到 `%rdx`；固定操作这两个寄存器 |
| 无符号除法前怎么准备被除数？ | `xorl %edx, %edx` 清空 `%rdx`（高 64 位 = 0） |
| `idivq` 之后商和余数在哪？ | 商 `%rax`，余数 `%rdx` |
| remdiv 里为什么先把 qp 从 `%rdx` 复制到 `%r8`？ | `idivq` 要占用 `%rdx:%rax` 当被除数，必须腾位置 |
| 除以 0 会发生什么？ | `SIGFPE` 异常（程序崩溃） |

> 本节配套练习是 3.9-3.12（书 §3.5.5 末尾），practice-problems.md 待补。

---

### 3.6 控制

到目前为止学过的指令都是**直线执行**：一条接一条往下走。但真正的程序到处是 if/else、循环、switch。CPU 要实现这些，需要两样东西：

1. **记住状态**：最近一次运算的结果是正是负？有没有溢出？——用「条件码」记
2. **按状态跳转**：根据条件码跳到不同的代码——用「跳转指令」（§3.6.3 起）

本节先讲条件码（3.6.1），它是后面一切控制流的地基。

### 3.6.1 条件码

CPU 内部维护着**四个 1 位标志位**（不是通用寄存器，不能直接 movq 读写）：

| 条件码 | 全称 | 含义 | 关心它的场合 |
|--------|------|------|-------------|
| `CF` | carry flag | 进位：最高位产生进位/借位 | **无符号数**溢出 |
| `ZF` | zero flag | 最近操作结果是否为 0 | 判断相等（cmp 之后看 ZF） |
| `SF` | sign flag | 结果是否为负（= 结果的最高位） | 判断正负 |
| `OF` | overflow flag | **有符号数**溢出（补码溢出） | 有符号数溢出 |

> CF 和 OF 是一对冤家：CF 管无符号溢出（比如 `0xFFFFFFFF + 1 = 0x100000000`，多出来的位丢进 CF），OF 管补码溢出（两个正数相加变成负数）。**同一个位模式，按无符号解释看 CF，按有符号解释看 OF**——这就是第 2 章讲的两种解释的分身。

**哪些指令设置条件码**：

- `addq / subq / negq / xorq / orq / andq` 等算术逻辑指令：设置（`leaq` 例外，见下）
- `imulq`：设置
- ⚠️ **`leaq` 不设置任何条件码**——它是地址计算指令，不是算术指令
- ⚠️ **`incq` / `decq` 设置 OF 和 ZF，但不设置 CF**——历史遗留的怪癖（8086 时代的设计），书里特意点名
- 移位指令：`CF` = 最后移出的那一位；`OF` 只在移位 1 位时有明确含义（左移 1 位 = 符号位是否改变），移多位不保证
- `movq` 等数据传送指令：**不设置**任何条件码
- 逻辑指令（`xorq` 等）会把 `CF` 和 `OF` 清零

**cmp 和 test：只设置条件码，不修改操作数**

| 指令 | 效果 | 用途 |
|------|------|------|
| `cmp S1, S2` | 按 **S2 − S1** 的结果设置条件码 | 比较两个数 |
| `test S1, S2` | 按 **S2 & S1** 的结果设置条件码 | 测试位 / 判零 |

> ⚠️ **`cmp` 的顺序是反的**（Intel 语法传统）：`cmpq %rax, %rbx` 算的是 **`%rbx - %rax`**，第二个减第一个。后面所有基于 cmp 的判断都按这个方向理解——`cmpq %rsi, %rdi` 之后 `jg` 跳，意思是"`%rdi > %rsi`"。

为什么要这两条"只出条件码、不出结果"的指令？因为 `if (x < y)` 只想**比较**，不想真的把 x 减掉 y——`cmp` 把减法"白算"一遍，只留下条件码，不破坏任何操作数。

**最常见的习语**：

```asm
testq %rax, %rax   # 按 %rax & %rax 设置条件码 → 判断 %rax 是 0 / 负 / 正
                   # ZF=1 → %rax == 0；SF=1 → 负数
```

```asm
cmpq  %rsi, %rdi   # 比较 %rdi 和 %rsi（算 %rdi - %rsi）
jg    .L1          # 若 %rdi > %rsi 则跳到 .L1（jg 在 §3.6.3 讲）
```

> 条件码怎么被"读出来"？下一节（3.6.2）讲 `set` 指令——把单个条件码翻译成 0/1 放进通用寄存器，那才是条件码和普通数据世界连接的桥。

---

### 3.6.1 自检

| 问题 | 答案 |
|------|------|
| `leaq 8(%rdi), %rax` 之后 ZF 会变吗？ | 不会——leaq 不设置任何条件码 |
| `cmpq %rax, %rbx` 算的是什么？ | `%rbx - %rax`（顺序和直觉相反，第二个减第一个） |
| `testq %rax, %rax` 在判断什么？ | `%rax` 是 0（ZF=1）还是负（SF=1）——自与判零/判负 |
| CF 和 OF 分别对应哪种溢出？ | CF 管**无符号**进位溢出，OF 管**补码**溢出 |
| 为什么 `incq` 不设置 CF？ | 历史包袱：8086 时代的设计遗留，书里特意点名了这个怪癖 |
| `movq` 会设置条件码吗？ | 不会——只有算术/逻辑指令设置 |
| `cmp` 和 `sub` 的区别？ | `cmp` 只设置条件码、不写回结果；`sub` 会把差写回目的操作数 |

---

### 3.6.2 访问条件码（Accessing the Condition Codes）

3.6.1 讲了"记账"：`cmp`/`test` 把比较结果写进条件码，但不碰操作数。这一节讲"取钱"：**怎么把条件码翻译成普通数据**。共有三种方式——本节讲 `set` 指令族，跳转（§3.6.3）和条件传送（§3.6.5）后面讲。

**set 指令**：`setX D`，D 只能是**单字节**寄存器（如 `%al`）或单字节内存。条件成立 → D=1，不成立 → D=0。

#### 完整 set 表（书 Table 3.5）

| 指令 | 同义名 | 含义 | 条件 |
|------|--------|------|------|
| `sete` | `setz` | 相等 / 为零 | `ZF` |
| `setne` | `setnz` | 不等 / 非零 | `~ZF` |
| `sets` | — | 负数 | `SF` |
| `setns` | — | 非负 | `~SF` |
| `setg` | `setnle` | 有符号大于 | `~(SF^OF) & ~ZF` |
| `setge` | `setnl` | 有符号大于等于 | `~(SF^OF)` |
| `setl` | `setnge` | 有符号小于 | `SF^OF` |
| `setle` | `setng` | 有符号小于等于 | `(SF^OF) \| ZF` |
| `seta` | `setnbe` | 无符号大于（above） | `~CF & ~ZF` |
| `setae` | `setnb` | 无符号大于等于 | `~CF` |
| `setb` | `setnae` | 无符号小于（below） | `CF` |
| `setbe` | `setna` | 无符号小于等于 | `CF \| ZF` |

#### 怎么背这张表——三个规律

**1. 看字母认条件码**：`e` 对 `ZF`，`s` 对 `SF`，无符号组（`a`=above / `b`=below）对 `CF`，有符号组（`g`=greater / `l`=less）对 `SF^OF`。无符号特意用 above/below 而不是 greater/less，就是为了和有符号区分开。

**2. 为什么只有 setg 和 seta 带 `& ~ZF`？** 因为 `~(SF^OF)` 和 `~CF` 这两个条件在"相等"时恰好成立（相等时 SF^OF=0、CF=0，取反都是 1），必须显式把"相等"排除掉才配得上"严格大于"。而 `setl`/`setb` 的条件（`SF^OF`/`CF`）在相等时天然为 0，不用管 ZF。`setle`/`setbe` 则反过来主动 `| ZF` 把相等算进去。

**3. 有符号看 OF，无符号看 CF。** 补码相加可能溢出到符号位"说谎"，要用 OF 纠偏（`SF^OF` 就是"符号位与溢出不一致"——3.6.1 记的账在这里用上）。无符号比较就是减完看有没有借位（CF）；OF 对无符号没意义——模 2^w 的环上转圈本来就不算错。

> 同一组操作数、同一条 `cmp`，`setl` 和 `setb` 可能给出**相反的答案**。a=0xFF、b=0x01：按有符号解释是 -1 < 1 → `setl` 得 1；按无符号解释是 255 < 1 不成立 → `setb` 得 0。**位模式本身不携带类型，用哪条 set 指令读它，就按哪种类型解释**——第 2 章的"两种解释"在指令集里长成了两条不同的指令。

#### 为什么 set 只写 1 个字节？

历史包袱：x86 从 8086 一路演化而来，set 指令的编码只支持字节大小的目标（§3.1 说的"臃肿"又一例）。所以 C 函数要返回 int 时，拿到 0/1 之后还要零扩展一步——`movzbl`（§3.4.2 学的）。

**例子 1（书 Figure 3.10）：`a < b`**

```c
int comp(long a, long b) {
    return a < b;      /* 返回 0 或 1 */
}
```

```asm
comp:
    cmpq    %rsi, %rdi    # 比较：算 %rdi - %rsi，只记账
    setl    %al           # SF^OF → %al（0 或 1）
    movzbl  %al, %eax     # 零扩展成 32 位 int（高 24 位补 0）
    ret
```

**例子 2（书 Figure 3.11）：`a >= b`——编译器省掉 movzbl 的花招**

```asm
comp:
    xorl    %eax, %eax    # %eax = 0，且高 32 位同时被清零
    cmpq    %rsi, %rdi    # 比较：只记账
    setge   %al           # ~(SF^OF) → %al，只写最低字节
    ret                   # 没有 movzbl！
```

关键规则：**x86-64 中，任何产生 32 位值的指令都会把寄存器的高 32 位清零**（§3.4.1 提过，`movl $0, %eax` 等价于 `movq $0, %rax` 的原因）。`xorl %eax, %eax` 先把整个 64 位 `%rax` 清零，`setge %al` 之后高 3 字节依然是 0——整个 `%rax` 已经是干净的 0/1，`movzbl` 就省了。所以你在反汇编里有时看到 `set + movzbl`，有时看到 `xorl + set`：**编译器在按"哪条路更省指令"二选一**。

> 自己动手验证：`gcc -Og -S` 编译 `return a >= b;` 和 `return a > b;`，对比哪个函数用了 xorl 花招。`practice/set_instr.c` 已备好 8 种比较，一次看全。

### 3.6.2 自检

| 问题 | 答案 |
|------|------|
| `sete` 看哪个条件码？`setb`？`setg`？ | `ZF`；`CF`；`~(SF^OF) & ~ZF` |
| 为什么 set 只写 1 字节，还要 `movzbl` 扩展？ | 历史包袱：set 指令编码只支持字节目标；返回 int 需零扩展到 32 位 |
| `setl` 和 `setb` 的区别？a=0xFF, b=0x01 时各得什么？ | 有符号小于（SF^OF）→ 1（-1<1）；无符号小于（CF）→ 0（255<1 不成立）——同一操作数相反答案 |
| 为什么 `setg` 带 `& ~ZF` 而 `setge` 不带？ | `~(SF^OF)` 在相等时成立，必须显式排除；setge 本来就要包含相等 |
| 有符号比较为什么必须看 OF？ | 补码溢出会让符号位"说谎"，`SF^OF` 用来纠偏 |
| Figure 3.11 的 `xorl %eax,%eax` 在干什么？ | 清零 %eax 同时清零高 32 位，让 `setge %al` 之后无需 movzbl |
| 书后练习 3.13、3.14 在哪？ | 本节末尾：3.13 填 set 指令，3.14 从 cmp 宽度反推数据类型——做完对书后答案 |

> 下节预告：set 只能把条件码变成数据。真正的 if/else、循环靠的是**跳转**（`jmp`/`jX` 指令，§3.6.3）——条件码的第二大用途。

---

### 3.6.3 跳转指令（Jump Instructions）

执行流**第一次会拐弯**。之前所有指令都是直线往下走；这一节引入两类跳转，把 3.6.1 记的账用于"改变流向"——这是 set（3.6.2）之外，条件码的第二大用途。

#### 无条件跳转 jmp

| 形式 | 例子 | 跳到哪里 |
|------|------|---------|
| 直接跳转 | `jmp .L1` | 标签 .L1 本身（目标编码在指令里） |
| 间接跳转 | `jmp *%rax` | 寄存器 `%rax` 里存的地址 |
| 间接跳转 | `jmp *(%rax)` | 内存 `(%rax)` 里存的地址 |

> 注意 `*` 记号：汇编里 `*` 后跟寄存器/内存表示"**跳转到那里存的值**"。间接跳转的用武之地：switch 的跳转表（§3.6.8）、函数指针调用。

#### 条件跳转 jX

条件成立 → 跳；不成立 → **顺序执行下一条**（fall through）。谓词与 3.6.2 的 set 表**完全一致**——同一个条件，`set` 写一个字节 0/1，`jX` 跳一个地方：

| 指令 | 同义名 | 跳转条件 | 含义 |
|------|--------|---------|------|
| `jmp` | — | 1 | 无条件 |
| `je` | `jz` | `ZF` | 相等 / 零 |
| `jne` | `jnz` | `~ZF` | 不等 / 非零 |
| `js` | — | `SF` | 负数 |
| `jns` | — | `~SF` | 非负 |
| `jg` | `jnle` | `~(SF^OF) & ~ZF` | 有符号大于 |
| `jge` | `jnl` | `~(SF^OF)` | 有符号大于等于 |
| `jl` | `jnge` | `SF^OF` | 有符号小于 |
| `jle` | `jng` | `(SF^OF) \| ZF` | 有符号小于等于 |
| `ja` | `jnbe` | `~CF & ~ZF` | 无符号大于（above） |
| `jae` | `jnb` | `~CF` | 无符号大于等于 |
| `jb` | `jnae` | `CF` | 无符号小于（below） |
| `jbe` | `jna` | `CF \| ZF` | 无符号小于等于 |

背表不用重复花力气：3.6.2 的三条规律原样适用（字母对条件码、为什么只有 jg/ja 带 `~ZF`、有符号看 OF 无符号看 CF）。

#### 怎么用起来——if/else 的最小形态

C 的 if/else 和循环就是"`cmp/test` + `jX` + `jmp`"的组合：比较记账（3.6.1）→ 条件跳转决定进哪块（本节）。3.6.5 会看到完整翻译，这里先看骨架：

```asm
    cmpq    %rsi, %rdi     # 比较：只记账
    jle     .L2            # %rdi <= %rsi → 跳去 .L2；不成立就顺序执行
    ; then 块
    jmp     .L3            # 无条件跳过 else 块
.L2:
    ; else 块
.L3:
```

> 想提前看一眼真身：拿一个 if/else 编译 `gcc -Og -S`，你会看到上面这个结构（跳转指令本身 Windows/Linux 完全一样）。

> 预告 §3.6.4：跳转指令在机器码里**怎么编码**——目标地址不是直接存进去的，而是存"离下一条指令的偏移"（PC 相对寻址）。书后练习 3.15（算跳转目标地址）需要那节的知识，等读完 3.6.4 一起做。

### 3.6.3 自检

| 问题 | 答案 |
|------|------|
| `jmp .L1`、`jmp *%rax`、`jmp *(%rax)` 各跳到哪里？ | 标签 .L1；`%rax` 里的值；内存 `(%rax)` 里的值（后两者是间接跳转） |
| 条件跳转的谓词和哪个指令族一样？`jge` 的条件？ | 和 set 完全一样；`~(SF^OF)` |
| 条件不成立时 jX 做什么？ | 什么也不做，顺序执行下一条（fall through） |
| C 的 if/else 对应什么跳转组合？ | `cmp/test` + 条件跳转（跳去 else）+ 无条件 `jmp`（跳过 else） |
| 哪些 C 构造用间接跳转？ | switch 跳转表、函数指针 |
| `je` 和 `jz` 什么关系？ | 同一条指令的两个名字（机器码完全相同） |

> 下节预告：3.6.4 跳转指令的编码——PC 相对寻址是本节配套的硬核细节，读完它 3.15 才做得动。

---

### 3.6.4 跳转指令的编码（Jump Instruction Encodings）

**一句话**：机器码里存的不是目标地址，而是**"离下一条指令有多远"**（偏移量）。

```
目标地址 = 下一条指令的地址 + 偏移量
```

向后跳 = 负偏移（补码表示）。

#### 先复习：objdump 一行有三列

```
   4:  78 01    js 7 <abs_value+0x7>
   │    │       │
   │    │       └─ 人话：js = "负数就跳"；7 = 跳转目标（objdump 已算好）
   │    └───────── 字节：这条指令的机器码——CPU 真正执行的就是它
   └────────────── 地址：这条指令在函数里的起点（字节编号）
```

> ⚠️ 两个坑：
> ① **地址是十六进制**——`19` 是 0x19 = 25（十进制），不是 19。手算用十六进制加减。
> ② 跳转目标 objdump 已经**替你算好**写在第三列了——手算完跟它对照，不一致就是你错了（自带答案的练习册）。

#### 指令 = 一排宽度不同的积木

每条指令占 1-15 字节不等；下一条的地址 = 前面所有指令的字节数之和。CPU 从左往右一块块读；**跳转 = 不往右移了，直接把读的位置改到目标**。

```
地址: 0     2     4      6   7     9    b
      ┌─────┬─────┬──────┬───┬─────┬─────┐
字节: │89 c8│85 c9│78 01 │c3 │f7 d8│eb fb│
      └─────┴─────┴──────┴───┴─────┴─────┘
人话:  mov   test   js   retq neg   jmp
      2字节 2字节  2字节 1字节 2字节 2字节
```

#### 自己机器的真实例子（practice/jump_enc.c，MinGW -Og）

```asm
abs_value:
   0:  89 c8        mov  %ecx,%eax
   2:  85 c9        test %ecx,%ecx
   4:  78 01        js   7        # 偏移 +1：下一条=6 → 6+1=7 ✓（向前跳）
   6:  c3           retq
   7:  f7 d8        neg  %eax
   9:  eb fb        jmp  6        # 偏移 fb=-5：下一条=0xb → 0xb-5=6 ✓（向后跳）

sum_to:
  17:  7f 07        jg   20       # 偏移 +7：下一条=0x19 → 0x19+7=0x20 ✓
  1e:  eb f5        jmp  15       # 偏移 f5=-0xB：下一条=0x20 → 0x20-0xB=0x15 ✓
```

拆解 `78 01`：`78` = 操作码（"我是 js"），`01` = 偏移量。基准点为什么是"下一条"：CPU 读完这条跳转指令后，读指针已经停在下一条的位置，偏移从这个位置开始算——这是编码时的约定。

#### 为什么这么设计

1. **省字节**：±128 以内 1 字节偏移就够（`78 01` 共 2 字节）；更远用 4 字节偏移版。存绝对地址要 8 字节。
2. **位置无关**（position-independent code）：代码不管被加载到内存哪里，指令间的相对距离不变，跳转永远正确——共享库、ASLR（地址随机化）全靠它。

> 你 objdump 的 `.o` 文件地址从 0 开始（还没链接）；可执行文件里是完整地址（如 `4003d5`）——算法完全一样。

#### 一个限制

**条件跳转只能 PC 相对**（不能间接跳转）；只有 `jmp` 支持间接目标（`jmp *%rax` / `jmp *(%rax)`，3.6.3 学过）。

### 3.6.4 自检

| 问题 | 答案 |
|------|------|
| `78 01` 的 78 和 01 分别是什么？ | 操作码（js 指令本身）/ 偏移 +1 |
| 偏移的基准点在哪？ | 跳转指令**下一条**的地址 |
| 向后跳的偏移是什么形式？ | 补码负数（如 `f5` = -0xB） |
| objdump 显示的地址是几进制？ | 十六进制（坑：0x19 = 25） |
| 为什么存偏移不存绝对地址？ | 省字节 + 位置无关代码 |
| 条件跳转能间接跳转吗？ | 不能——只有 jmp 支持 |
| 书后练习 3.15 | je/jb/ja 目标计算，同一规则，跟 objdump 对照自查 |

> 下节预告：3.6.5 用条件控制实现条件分支——if/else 的完整翻译（goto 版本），前面三节的所有零件第一次组装起来。
