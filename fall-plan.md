# 大二上规划 · 三模块项目（2026/9 — 2027/1）

> 一个仓库，三个产品：自研 allocator → mini-Tensor 库 → GEMM 优化。
> 简历一句话：「C 实现 CPU 张量库，存储后端为自研 allocator（free list + 边界合并 + 分箱优化，对比 ptmalloc 差距分析），GEMM 分块/SIMD/多线程优化」。
>
> ⚠️ 目标不是"达到 ptmalloc 水平"（那是 30 年工业工程，不可能也不必要）——目标是**读懂 ptmalloc 核心设计、移植 1 个机制（分箱）、能讲清差距**。

---

## 最终产品形态

```
csapp-lab/
├── malloc/                    ← 产品 1：my_malloc.h/.c + test_random + ptmalloc-对比.md
├── tensor/                    ← 产品 2：tensor.h/.c + ops + demo.c（两层 MLP 前向）
│   ├── gemm_naive.c           ← 产品 3：4 个 GEMM 版本
│   ├── gemm_blocked.c
│   ├── gemm_simd.c
│   ├── gemm_parallel.c
│   └── bench_gemm.c
└── README.md                  ← 首页：架构图 + 性能表
```

关系：my_malloc 分配 Tensor 的 data → 张量库调用 gemm。

---

## 验收标准（三个数字，缺一个就不算完成）

1. **GEMM 提速表**：naive → +分块 → +SIMD → +多线程，每一级提速都有 benchmark 数字和"为什么快"的解释
2. **allocator 测试**：随机 10000 次 alloc/free/realloc 零错误，valgrind/ASan 零泄漏
3. **ptmalloc 对比文档**：能讲清我的合并策略 vs glibc 的 bins 设计，至少 3 个具体差异

---

## 时间表（16 周，周粒度）

> 排序原则：**写线从实际水平起步，不跳级**。malloc 的真实前置是：裸指针手感 → 链表（free list 就是链表）→ heap/sbrk 概念。写线先花两周热身，热身内容本身就是 malloc 的前置零件，不是绕路。读线（Ch3）与写线互不依赖，从 W2 起每天各 1.5 小时。

| 周 | 日期 | 读线（1.5h/天） | 写线（1.5h/天） |
|----|------|----------------|----------------|
| W1 | 9/1-9/6 | Ch3 笔记 3.1-3.5.5 自检题全过 | 重做 2.63 / 2.78 |
| W2 | 9/7-9/13 | §3.6.1-3.6.6 条件码+跳转 | **指针热身**：手写 strlen/strcpy/strcmp + 指针遍历数组 |
| W3 | 9/14-9/20 | §3.6.7-3.6.8 循环 + §3.7 过程 | **单链表全套**：create/append/insert/delete/free + 打印 + 测试 |
| W4 | 9/21-9/27 | §3.8 数组 + §3.9 struct；**§9.1 + §9.9.1-9.9.2（heap/sbrk）** | **sbrk 实验**：打印 sbrk(0)、申请一块、看堆增长 + malloc v0 块结构定义 + gdb 基础 |
| W5 | 9/28-10/4 | §3.10 缓冲区溢出 + Ch3 总复习；**§9.9.3-9.9.5（隐式链表、边界标记）** | malloc v0 跑通：first-fit 分配 |
| W6 | 10/5-10/11 | 国庆：Ch3 练习题补做；**§9.9.6-9.9.9（放置策略、切分、合并）** | malloc v1：free + 切分 + 右侧合并 |
| W7 | 10/12-10/18 | Ch6.1-6.3 缓存；**§9.9.10-9.9.11（显式空闲链表）** | malloc v2：边界标记左右合并 + realloc |
| W8 | 10/19-10/25 | Ch6.4-6.6 + Ch5 快速过；**§9.9.12（分离链表＝分箱）+ §9.11（内存 bug 手册）** | malloc 测试：随机 10000 + valgrind + ptmalloc 对比文档 |
| W9 | 10/26-11/1 | 分箱移植（加分项，没时间放 W16 缓冲周） | Tensor v0：结构体 + my_malloc 存储 + 打印 |
| W10 | 11/2-11/8 | 复盘/文档 | Tensor v1：逐元素算子 + broadcast + view |
| W11 | 11/9-11/15 | 复盘/文档 | Tensor v2：朴素 matmul + 正确性测试 |
| W12 | 11/16-11/22 | 复盘/文档 | GEMM 分块 |
| W13 | 11/23-11/29 | 复盘/文档 | GEMM SIMD（AVX2） |
| W14 | 11/30-12/6 | 复盘/文档 | GEMM 多线程 + benchmark 四版本 |
| W15 | 12/7-12/13 | — | MLP demo + README/性能表 |
| W16 | 12/14-12/20 | 缓冲周；**之后期末冻结，课程优先** | 简历草稿 + 补漏洞 |

**里程碑检查点**：W3 末链表完成 → W5 末 Ch3 + malloc v0 → W8 末 malloc 全线（含 ptmalloc 对比）→ W14 末 GEMM 完成 → W16 末简历草稿。

> 9 月结束时你手上就有：**Ch3 完整笔记 + 链表全套 + 能跑的 my_malloc v0**。从 W2 起每周都有真实代码产出。

## 章节地图（哪些章要看、看多少）

| 章 | 看多少 | 为什么 |
|----|--------|--------|
| 第 3 章 | 全看（W1-W5） | 汇编——调试、性能分析的基础 |
| 第 9 章 | **§9.1 + §9.9 全节 + §9.11**（W4-W8 跟着写线走） | §9.9「动态内存分配」就是 malloc 的教材；分箱 = §9.9.12 |
| 第 6 章 | 全看（W7-W8） | GEMM 分块 = 吃缓存，Ch6 是先修 |
| 第 5 章 | 快速过（W8） | 性能测量方法 |
| 第 8 章 + §9.2-9.8 | 寒假 | 进程、页表/TLB——面试知识，不挡项目 |
| 第 4、7 章 | 跳过 | Y86 处理器设计、链接——不影响主线 |

---

## 每天怎么过（3-4 小时）

- 1.5h：CS:APP 阅读 + 笔记（周任务对应章节）
- 1.5h：写代码（malloc/tensor 周任务）
- 0.5h：整理 commit + push

**最低要求**：打开笔记或代码 10 分钟 + 一条 commit。做多算赚。

---

## 保险丝：学期中途被课程压住时，按顺序砍

1. 砍 GEMM 多线程 → 2. 砍 SIMD → 3. 砍 MLP demo → 4. 砍 realloc

**保底线（砍完也必须完成）**：malloc 核心（alloc/free/合并）+ Tensor v0-v2 + GEMM 分块。
保底线 = 简历上依然成立的项目。

---

## 铁律

1. **课程和 GPA 永远优先**。期中/期末周计划冻结，之后顺延，不算断档
2. **断档不补旧债**：第二天从当天周任务继续；连续 3 天连最低要求都没做到，来找我
3. **国庆（10/1-10/7）是加速器**：时间多可以提前，但不加任务——提前完成就休息
4. **不做 autograd、不做完整算子库**——那是寒假/大二下的事，这学期碰了必翻车

---

## 寒假预告（2027/1 底-2 月）

- GEMM CUDA 版（Colab T4 或学校机器）——简历上加"GPU 优化"一行
- CS:APP 第 8、9 章完整读（进程 + 虚拟内存）
- 开始收集面试八股

---

*计划定于 2026-09-01。执行从 W1 复习收尾开始。*
